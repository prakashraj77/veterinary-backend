spring.application.name=backend

server.port=8080



spring.datasource.url=jdbc:mysql://localhost:3306/vetcare

spring.datasource.username=root
spring.datasource.password=Vasanth@zenve

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver



spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true

spring.jpa.properties.hibernate.format_sql=true

spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect



spring.web.error.include-message=always
spring.web.error.include-binding-errors=always



app.jwt.secret=zenve-vetcare-doctor-portal-super-secret-signing-key-change-me
app.jwt.expiration-ms=86400000



# Zenve admin backend (approval workflow).
# - base-url: where this app POSTs new doctor registrations to (see AdminApprovalClient).
# - internal-secret: shared secret the admin backend must send back on
#   /api/internal/doctors/status so only it can update approval status here.
app.admin.base-url=${ADMIN_BASE_URL:http://localhost:5000/api}
app.admin.internal-secret=${ADMIN_INTERNAL_SECRET:change-this-shared-secret}



# --- Outbound email (forgot-password reset links, prescription emails) ---
# Leave MAIL_HOST unset in dev: emails then just get logged instead of sent,
# so forgot-password/reset-password still work without an SMTP account.
# For real delivery, set these to your SMTP provider, e.g. Gmail:
#   MAIL_HOST=smtp.gmail.com MAIL_PORT=587 MAIL_USERNAME=you@gmail.com
#   MAIL_PASSWORD=<16-char app password>  (not your normal Gmail password)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=vasanthzenve@gmail.com
spring.mail.password=bnet trqf fwxg qhds

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000

app.mail.from=vasanthzenve@gmail.com

# Public URL of vd-dashboard, used to build the link in reset-password emails.
app.frontend.base-url=${FRONTEND_BASE_URL:http://localhost:5173}



# --- SMS / WhatsApp via Twilio ---
# Leave these unset in dev: sends are then logged with status
# NOT_CONFIGURED instead of silently pretending to succeed.
# Get real values from https://console.twilio.com
twilio.account-sid=${TWILIO_ACCOUNT_SID:}
twilio.auth-token=${TWILIO_AUTH_TOKEN:}
twilio.sms-from=${TWILIO_SMS_FROM:}
twilio.whatsapp-from=${TWILIO_WHATSAPP_FROM:}